---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: patrickcrocker/cloudfoundry-cli
    tag: 'latest'

inputs:
- name: pipeline
- name: release

outputs:
- name: itest

params:
  CF_API_URL:
  CF_USERNAME:
  CF_PASSWORD:
  CF_SKIP_SSL:
  CF_ORG:
  CF_SPACE:
  CF_APP_NAME:
  CF_APP_SERVICES:
  CF_ARTIFACT_ID:

run:
  path: pipeline/ci/itest.sh
